# Dockerfile_test
FROM python:3.9-slim

WORKDIR /app

# Copier tous les fichiers nécessaires depuis le contexte (jenkins/tests)
COPY *.py .
COPY requirements.txt .
COPY ./ /app/tests/

# Installer les dépendances
RUN pip install --no-cache-dir -r requirements.txt